[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  AI_PROVIDER = "mock"

[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888
  framework = "#auto"
